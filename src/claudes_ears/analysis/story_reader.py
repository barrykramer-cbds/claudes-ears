"""Grounded story reading (rule-based synthesis) — stub."""

from __future__ import annotations


def analyze(context: dict[str, object]) -> dict[str, object]:
    """Synthesize a musically-grounded story from upstream analysis dicts (no LLM).

    ``context`` is keyed by depends_on step name.
    """
    raise NotImplementedError
